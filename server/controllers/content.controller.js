// Rewrite/server/controllers/content.controller.js
import asyncHandler from 'express-async-handler';
import { body, param, query, validationResult } from 'express-validator';
import mongoose from 'mongoose';
import Content from '../models/content.model.js';
import User from '../models/user.model.js';
import AppError from '../utils/AppError.js';
import bcrypt from 'bcryptjs';
//import { format } from 'date-fns'; // Import for sitemap date formatting

// ... (All other controller functions like getFilteredContent, getMyPageFeed, etc., remain here) ...
const checkValidation = (req, next) => { const errors = validationResult(req); if (!errors.isEmpty()) { const errorMessages = errors.array().map(err => err.msg).join(', '); next(new AppError(errorMessages, 400)); return false; } return true; };
const getFilteredContent = asyncHandler(async (req, res, next) => { const page = parseInt(req.query.page, 10) || 1; const limit = parseInt(req.query.limit, 10) || 10; const skip = (page - 1) * limit; const view = req.query.view; const feedType = req.query.feedType; const sortBy = req.query.sortBy; const filter = {}; if (req.query.parentContent === 'null') filter.parentContent = null; else if (mongoose.Types.ObjectId.isValid(req.query.parentContent)) filter.parentContent = req.query.parentContent; else if (!req.query.parentContent) filter.parentContent = null; if (feedType === 'popular' || (filter.parentContent === null && !feedType && sortBy !== 'truePopularity_desc')) { filter.isPrivateToFollowers = false; } if (sortBy === 'truePopularity_desc') { filter.parentContent = null; filter.isPrivateToFollowers = false; } let articles = []; let totalArticles = 0; let selection = 'id title text author parentContent createdAt updatedAt likeCount likes isPrivateToFollowers reports isReported'; if (view === 'titles' && filter.parentContent === null) { selection = 'id title author createdAt isPrivateToFollowers likeCount'; } if (sortBy === 'truePopularity_desc') { const aggregationPipeline = [ { $match: { parentContent: null, isPrivateToFollowers: false } }, { $graphLookup: { from: 'contents', startWith: '$_id', connectFromField: '$_id', connectToField: 'parentContent', as: 'lineage', } }, { $addFields: { truePopularityScore: { $add: [ '$likeCount', { $sum: { $map: { input: '$lineage', as: 'descendant', in: '$$descendant.likeCount' } } } ] } } }, { $sort: { truePopularityScore: -1, createdAt: -1 } }, { $facet: { paginatedResults: [ { $skip: skip }, { $limit: limit }, { $lookup: { from: 'users', localField: 'author', foreignField: '_id', as: 'authorInfo' }}, { $unwind: { path: '$authorInfo', preserveNullAndEmptyArrays: true } }, { $project: { _id: 1, id: '$_id', title: 1, text: 1, parentContent: 1, createdAt: 1, updatedAt: 1, likeCount: 1, likes:1, isPrivateToFollowers: 1, reports:1, isReported: 1, truePopularityScore: 1, author: { _id: '$authorInfo._id', id: '$authorInfo._id', username: '$authorInfo.username', profilePicture: '$authorInfo.profilePicture', isVerified: '$authorInfo.isVerified' }, } } ], totalCount: [ { $count: 'count' } ] } } ]; const results = await Content.aggregate(aggregationPipeline); articles = results[0].paginatedResults.map(doc => ({...doc, id: doc._id.toString()})); totalArticles = results[0].totalCount.length > 0 ? results[0].totalCount[0].count : 0; } else { let sortOptions = { createdAt: -1 }; if (sortBy === 'likes_desc' || feedType === 'popular') sortOptions = { likeCount: -1, createdAt: -1 }; else if (sortBy === 'createdAt_asc') sortOptions = { createdAt: 1 }; const articlesQuery = Content.find(filter).sort(sortOptions).skip(skip).limit(limit).populate('author', 'username profilePicture isVerified').select(selection); articles = await articlesQuery; totalArticles = await Content.countDocuments(filter); } res.json({ articles, page, pages: Math.ceil(totalArticles / limit), totalArticles }); });
const getMyPageFeed = asyncHandler(async (req, res, next) => { const currentUserId = req.user.id; const page = parseInt(req.query.page, 10) || 1; const limit = parseInt(req.query.limit, 10) || 10; const skip = (page - 1) * limit; const currentUser = await User.findById(currentUserId).select('following'); if (!currentUser || !currentUser.following || currentUser.following.length === 0) { return res.json({ articles: [], page: 1, pages: 0, totalArticles: 0 }); } const followedUserIds = currentUser.following; const filter = { author: { $in: followedUserIds }, parentContent: null }; const articles = await Content.find(filter).sort({ createdAt: -1 }).skip(skip).limit(limit).populate('author', 'username profilePicture isVerified').select('id title text author parentContent createdAt updatedAt likeCount likes isPrivateToFollowers reports isReported'); const totalArticles = await Content.countDocuments(filter); res.json({ articles, page, pages: Math.ceil(totalArticles / limit), totalArticles }); });
const getExploreFeed = asyncHandler(async (req, res, next) => { const currentUserId = req.user.id; const page = parseInt(req.query.page, 10) || 1; const limit = parseInt(req.query.limit, 10) || 20; const skip = (page - 1) * limit; const currentUser = await User.findById(currentUserId).select('following'); const myFollowingObjectIds = currentUser ? currentUser.following : []; const secondDegreeUsers = await User.aggregate([ { $match: { _id: { $in: myFollowingObjectIds } } }, { $lookup: { from: 'users', localField: 'following', foreignField: '_id', as: 'secondDegreeFollowingArr' }}, { $unwind: '$secondDegreeFollowingArr' }, { $replaceRoot: { newRoot: '$secondDegreeFollowingArr' } }, { $match: { _id: { $nin: [new mongoose.Types.ObjectId(currentUserId), ...myFollowingObjectIds] }, status: 'active', isPrivate: false }}, { $group: { _id: '$_id' } }, { $project: { _id: 1 } } ]); const secondDegreeUserIds = secondDegreeUsers.map(u => u._id); let articles = []; let totalArticles = 0; let feedResultType = 'second_degree'; if (secondDegreeUserIds.length > 0) { const exploreFilter = { author: { $in: secondDegreeUserIds }, parentContent: null, isPrivateToFollowers: false }; articles = await Content.find(exploreFilter).sort({ createdAt: -1 }).skip(skip).limit(limit).populate('author', 'username profilePicture isVerified').select('id title text author parentContent createdAt updatedAt likeCount likes isPrivateToFollowers reports isReported'); totalArticles = await Content.countDocuments(exploreFilter); } if (!articles || articles.length < limit) { const articlesNeeded = limit - (articles ? articles.length : 0); if (articlesNeeded > 0) { feedResultType = articles && articles.length > 0 ? 'second_degree_with_fallback' : 'popular_fallback'; const existingAuthorIds = articles ? articles.map(a => a.author.id.toString()) : []; const popularExploreFilter = { author: { $nin: [currentUserId, ...myFollowingObjectIds.map(id=>id.toString()), ...secondDegreeUserIds.map(id=>id.toString()), ...existingAuthorIds] }, parentContent: null, isPrivateToFollowers: false, }; const fallbackArticles = await Content.find(popularExploreFilter).sort({ likeCount: -1, createdAt: -1 }).limit(articlesNeeded).populate('author', 'username profilePicture isVerified').select('id title text author parentContent createdAt updatedAt likeCount likes isPrivateToFollowers reports isReported'); articles = articles ? [...articles, ...fallbackArticles] : fallbackArticles; if (feedResultType === 'popular_fallback') totalArticles = await Content.countDocuments(popularExploreFilter); else totalArticles = articles.length + ( (page === Math.ceil(totalArticles/limit)) ? 0 : articlesNeeded); } } res.json({ articles, page, pages: Math.ceil(totalArticles / limit), totalArticles, type: feedResultType }); });
const getContentById = [ param('id').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const content = await Content.findById(req.params.id).populate('author', 'username profilePicture isVerified').populate('likes', 'id username').populate('reports.reporter', 'id username'); if (!content) return next(new AppError('Content not found', 404)); res.json(content); }), ];
const getContentLineage = [ param('id').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const lineage = []; let currentContentId = req.params.id; const MAX_DEPTH = 15; for (let i = 0; i < MAX_DEPTH && currentContentId; i++) { const contentItem = await Content.findById(currentContentId).populate('author', 'username profilePicture isVerified').populate('reports.reporter', 'id').select('id title text author parentContent createdAt updatedAt likeCount likes isPrivateToFollowers reports isReported'); if (!contentItem) break; lineage.push(contentItem); const topChild = await Content.findOne({ parentContent: contentItem.id, isPrivateToFollowers: false }).sort({ likeCount: -1, createdAt: 1 }).select('id'); currentContentId = topChild ? topChild.id : null; } if (lineage.length === 0) return next(new AppError('Content not found', 404)); res.json(lineage); }), ];
const getContentVersions = [ param('id').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const contentItem = await Content.findById(req.params.id).select('parentContent'); if (!contentItem || !contentItem.parentContent) return res.json([]); const versions = await Content.find({ parentContent: contentItem.parentContent, _id: { $ne: req.params.id }, isPrivateToFollowers: false }).sort({ likeCount: -1, createdAt: 1 }).populate('author', 'username profilePicture isVerified').select('id title text author parentContent createdAt updatedAt likeCount likes isPrivateToFollowers reports isReported'); res.json(versions); }), ];
const createContent = [ body('text').trim().isLength({ min: 1, max: 10000 }), body('title').optional().trim().isLength({ max: 150 }), body('parentContent').optional({ checkFalsy: true }).isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const { title, text, parentContent } = req.body; const newContentData = { author: req.user.id, text: text.trim() }; if (title && !parentContent) newContentData.title = title.trim(); if (parentContent) { const parent = await Content.findById(parentContent); if (!parent) return next(new AppError('Parent content not found', 404)); newContentData.parentContent = parentContent; } const content = await Content.create(newContentData); const populatedContent = await Content.findById(content.id).populate('author', 'username profilePicture isVerified').populate('likes', 'id username'); res.status(201).json(populatedContent); }), ];
const updateContent = [ param('id').isMongoId(), body('text').trim().isLength({ min: 1, max: 10000 }), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const { text } = req.body; const content = await Content.findById(req.params.id); if (!content) return next(new AppError('Content not found', 404)); if (content.author.toString() !== req.user.id.toString() && req.user.role !== 'admin') return next(new AppError('Not authorized', 403)); content.text = text.trim(); const updatedContent = await content.save(); const populatedContent = await Content.findById(updatedContent.id).populate('author', 'username profilePicture isVerified').populate('likes', 'id username'); res.json(populatedContent); }), ];
const toggleLikeContent = [ param('id').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const content = await Content.findById(req.params.id); if (!content) return next(new AppError('Content not found', 404)); const userId = req.user.id; const alreadyLikedIndex = content.likes.findIndex(like => like.toString() === userId.toString()); if (alreadyLikedIndex > -1) content.likes.splice(alreadyLikedIndex, 1); else content.likes.push(userId); await content.save(); res.json({ id: content.id, likes: content.likes, likeCount: content.likeCount }); }), ];
const reportContent = [ param('id').isMongoId(), body('reason').optional().trim().isLength({ max: 200 }), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const { reason } = req.body; const content = await Content.findById(req.params.id); if (!content) return next(new AppError('Content not found', 404)); const userId = req.user.id; const alreadyReportedByThisUser = content.reports.find(report => report.reporter.toString() === userId.toString()); if (alreadyReportedByThisUser) return next(new AppError('You have already reported this content', 400)); content.reports.push({ reporter: userId, reason: reason || "No reason provided" }); await content.save(); const updatedContent = await Content.findById(content.id).populate('author', 'username profilePicture isVerified').populate('reports.reporter', 'id username'); res.json(updatedContent); }), ];
const unreportContent = [ param('id').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const content = await Content.findById(req.params.id); if (!content) return next(new AppError('Content not found', 404)); const reportIndex = content.reports.findIndex(report => report.reporter.toString() === req.user.id.toString()); if (reportIndex === -1) return res.json({ message: 'No active report by you found for this content.', contentId: content.id, isReported: content.isReported, reportsCount: content.reports.length, currentUserReported: false }); content.reports.splice(reportIndex, 1); await content.save(); const updatedContent = await Content.findById(content.id).populate('author', 'username profilePicture isVerified').populate('reports.reporter', 'id username'); res.json(updatedContent); }) ];
const getArticlesByUser = [ param('userId').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const authorId = req.params.userId; const page = parseInt(req.query.page, 10) || 1; const limit = parseInt(req.query.limit, 10) || 15; const skip = (page - 1) * limit; const authorProfile = await User.findById(authorId).select('isPrivate followers'); if (!authorProfile) return next(new AppError('Author not found', 404)); const filter = { author: authorId, parentContent: null }; const currentUserId = req.user ? req.user.id : null; let canViewPrivateArticles = false; if (currentUserId) { if (currentUserId === authorId.toString() || req.user.role === 'admin') canViewPrivateArticles = true; else if ((authorProfile.followers || []).some(followerId => followerId.toString() === currentUserId)) canViewPrivateArticles = true; } if (!canViewPrivateArticles) filter.isPrivateToFollowers = false; const articles = await Content.find(filter).sort({ createdAt: -1 }).skip(skip).limit(limit).populate('author', 'username profilePicture isVerified').select('id title createdAt isPrivateToFollowers likeCount text'); const totalArticles = await Content.countDocuments(filter); res.json({ articles, page, pages: Math.ceil(totalArticles / limit), totalArticles }); }) ];
const toggleArticlePrivacy = [ param('articleId').isMongoId(), body('isPrivateToFollowers').isBoolean(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const articleId = req.params.articleId; const { isPrivateToFollowers } = req.body; const currentUserId = req.user.id; const article = await Content.findById(articleId); if (!article) return next(new AppError('Article not found', 404)); if (article.author.toString() !== currentUserId && req.user.role !== 'admin') return next(new AppError('Not authorized', 403)); article.isPrivateToFollowers = isPrivateToFollowers; await article.save(); res.json({ message: 'Privacy updated', articleId: article.id, isPrivateToFollowers: article.isPrivateToFollowers }); }) ];
const getAllContentForAdmin = asyncHandler(async (req, res, next) => { const page = parseInt(req.query.page, 10) || 1; const limit = parseInt(req.query.limit, 10) || 500; const skip = (page - 1) * limit; const allContent = await Content.find({}).sort({ createdAt: -1 }).populate('author', 'username').select('id title text author parentContent createdAt updatedAt likeCount isReported reports').skip(skip).limit(limit); const totalContent = await Content.countDocuments({}); res.json({ content: allContent.map(c => ({ ...c.toJSON(), id: c._id.toString(), reportsCount: (c.reports || []).length })), page, pages: Math.ceil(totalContent / limit), totalContent }); });
const deleteContentForAdmin = [ param('id').isMongoId(), asyncHandler(async (req, res, next) => { if (!checkValidation(req, next)) return; const contentIdToDelete = req.params.id; const content = await Content.findById(contentIdToDelete); if (!content) return next(new AppError('Content not found', 404)); const deleteChildrenRecursive = async (parentId) => { const children = await Content.find({ parentContent: parentId }).select('_id'); for (const child of children) { await deleteChildrenRecursive(child.id); await Content.deleteOne({ _id: child.id }); console.log(`Admin deleted child: ${child.id}`); } }; await deleteChildrenRecursive(contentIdToDelete); await Content.deleteOne({ _id: contentIdToDelete }); console.log(`Admin deleted: ${contentIdToDelete}`); res.json({ message: `Content deleted` }); }), ];

// @desc    Generate a dynamic sitemap
// @route   GET /api/content/sitemap.xml
// @access  Public
const getSitemap = asyncHandler(async (req, res, next) => {
    const baseURL = process.env.CLIENT_URL || 'https://rewrite-9ers.onrender.com';
    let xml = '<?xml version="1.0" encoding="UTF-8"?>';
    xml += '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">';

    // 1. Add static pages
    xml += `<url><loc>${baseURL}/</loc><changefreq>daily</changefreq><priority>1.0</priority></url>`;
    xml += `<url><loc>${baseURL}/explore</loc><changefreq>daily</changefreq><priority>0.8</priority></url>`;

    // 2. Add public user profiles
    const publicUsers = await User.find({ isPrivate: false, status: 'active' }).select('username updatedAt');
    for (const user of publicUsers) {
        xml += `<url><loc>${baseURL}/profile/${user.username}</loc><lastmod>${new Date(user.updatedAt).toISOString()}</lastmod><changefreq>weekly</changefreq><priority>0.6</priority></url>`;
    }

    // 3. Add public top-level articles
    const publicArticles = await Content.find({ isPrivateToFollowers: false, parentContent: null }).select('id updatedAt');
    for (const article of publicArticles) {
        // Use the /read/ path which is handled by your React Router
        xml += `<url><loc>${baseURL}/read/${article.id}</loc><lastmod>${new Date(article.updatedAt).toISOString()}</lastmod><changefreq>monthly</changefreq><priority>0.5</priority></url>`;
    }

    xml += '</urlset>';
    res.header('Content-Type', 'application/xml');
    res.send(xml);
});

export {
  getFilteredContent,
  getMyPageFeed,
  getExploreFeed,
  getContentById,
  getContentLineage,
  getContentVersions,
  createContent,
  updateContent,
  toggleLikeContent,
  reportContent,
  unreportContent,
  getArticlesByUser,
  toggleArticlePrivacy,
  getAllContentForAdmin,
  deleteContentForAdmin,
  getSitemap,
};